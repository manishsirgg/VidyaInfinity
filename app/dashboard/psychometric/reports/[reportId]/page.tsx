import Link from "next/link";
import { redirect } from "next/navigation";
import { requireUser } from "@/lib/auth/get-session";
import { createClient } from "@/lib/supabase/server";

export default async function Page({params}:{params:Promise<{reportId:string}>}){const {reportId}=await params; const {profile}=await requireUser(); const supabase=await createClient(); const {data:report}=await supabase.from("psychometric_reports").select("*,psychometric_tests(title),profiles(full_name)").eq("id",reportId).maybeSingle(); if(!report) redirect("/dashboard/psychometric"); if(profile.role!=="admin"&&report.user_id!==profile.id) redirect("/dashboard/psychometric"); const dim=report.dimension_scores&&typeof report.dimension_scores==="object"?Object.entries(report.dimension_scores as Record<string,{score:number;maxScore:number;percentage:number}>):[]; return <div className="mx-auto max-w-4xl px-4 py-8"><div className="rounded-xl border bg-white p-6"><p className="text-xs uppercase tracking-wide text-brand-600">Vidya Infinity</p><h1 className="text-2xl font-semibold">{report.psychometric_tests?.title??"Psychometric Report"}</h1><p className="text-sm text-slate-600">Student: {report.profiles?.full_name??"Student"} · Generated: {new Date(report.generated_at).toLocaleString()}</p><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><div>Total: {report.total_score}</div><div>Max: {report.max_score}</div><div>Percentage: {report.percentage_score}%</div><div>Band: {report.result_band??"N/A"}</div></div><p className="mt-4"><b>Summary:</b> {report.summary}</p><p><b>Strengths:</b> {Array.isArray(report.strengths)?report.strengths.join(", "):report.strengths}</p><p><b>Improvement Areas:</b> {Array.isArray(report.improvement_areas)?report.improvement_areas.join(", "):report.improvement_areas}</p><p><b>Recommendations:</b> {Array.isArray(report.recommendations)?report.recommendations.join(", "):report.recommendations}</p><div className="mt-4"><h2 className="font-medium">Dimension Scores</h2>{dim.map(([k,v])=><div key={k} className="text-sm">{k}: {v.score}/{v.maxScore} ({v.percentage}%)</div>)}</div><p className="mt-4 text-xs text-slate-500">Disclaimer: This report is for educational and guidance purposes only.</p><div className="mt-5 flex gap-2"><a href={`/api/psychometric/reports/${reportId}/download`} className="rounded bg-brand-600 px-3 py-2 text-sm text-white">Download PDF</a><Link href="/dashboard/psychometric" className="rounded border px-3 py-2 text-sm">Back to Dashboard</Link></div></div></div>}
